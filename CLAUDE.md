# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project State

This is a new, empty project workspace. No application framework, build system, or source code exists yet.

## Installed Skills

The `frontend-design` skill is installed (`.agents/skills/frontend-design/`). Use it when creating web components, pages, or UI layouts to produce distinctive, production-grade interfaces. It is automatically triggered for frontend UI tasks.

## Skills Management

Installed skills are tracked in `skills-lock.json`. The `.agents/skills/` directory contains skill definitions, and `.claude/skills/` is reserved for local skill overrides.
